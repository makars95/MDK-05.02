<Window x:Class="OptTorg.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:OptTorg.ViewModels"
        xmlns:converters="clr-namespace:OptTorg.Converters"
        Title="Авторизация в системе OptTorgKrask"
        Height="770" 
        Width="600"
        MinHeight="600" 
        MinWidth="500"
        Background="{DynamicResource DarkBgMedium}"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" 
                Style="{DynamicResource LogoBorderStyle}"
                Margin="0,15,0,15">
            <Image Source="Resources/Images/logo2.png" 
                   Style="{DynamicResource RoundedImageStyle}"/>
        </Border>

        <Border Grid.Row="1" 
                Margin="0,0,0,20"
                HorizontalAlignment="Center"
                Width="430">
            <TextBlock Text="🔐 Авторизация в системе" 
                       Style="{DynamicResource FormTitleStyle}"/>
        </Border>

        <StackPanel Grid.Row="2" 
                    Margin="0,0,0,10" 
                    x:Name="LoginPanel"
                    HorizontalAlignment="Center">
            <Border Style="{DynamicResource FormBorderStyle}">
                <StackPanel Width="380">
                    <TextBlock Text="👤 Логин" 
                               Style="{StaticResource FieldLabelStyle}"/>
                    <TextBox x:Name="LoginTextBox" 
                             Style="{DynamicResource BaseTextBoxStyle}"
                             Margin="0,0,0,5"
                             Width="380"/>

                    <TextBlock Text="🔒 Пароль" 
                               Style="{DynamicResource FieldLabelStyle}"
                               Margin="0,5,0,5"/>

                    <Grid Margin="0,0,0,20" Width="380">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <PasswordBox x:Name="PasswordBox" 
                                     Style="{DynamicResource BasePasswordBoxStyle}"
                                     Grid.Column="0"
                                     Width="335"/>

                        <TextBox x:Name="PasswordVisibleTextBox" 
                                 Style="{DynamicResource BaseTextBoxStyle}"
                                 Grid.Column="0"
                                 Width="335"
                                 Visibility="Collapsed"/>

                        <Button x:Name="TogglePasswordButton" 
                                Content="👁️" 
                                Style="{DynamicResource TogglePasswordButtonStyle}"
                                Grid.Column="1"
                                Margin="8,0,0,0"
                                Click="TogglePasswordVisibility_Click"/>
                    </Grid>

                    <Button x:Name="LoginButton" 
                            Content="ВОЙТИ" 
                            Style="{DynamicResource LoginButtonStyle}"
                            Margin="0,5,0,10"
                            Width="380"
                            Click="LoginButton_Click"/>

                    <TextBlock Text="Нет аккаунта? Зарегистрируйтесь" 
                               Style="{DynamicResource LinkTextStyle}"
                               MouseLeftButtonDown="ShowRegistrationPanel"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Форма регистрации (скрыта по умолчанию) -->
        <StackPanel Grid.Row="2" 
                    Margin="0,0,0,10" 
                    x:Name="RegisterPanel" 
                    Visibility="Collapsed"
                    HorizontalAlignment="Center">
            <Border Style="{DynamicResource FormBorderStyle}">
                <ScrollViewer VerticalScrollBarVisibility="Auto" 
                              MaxHeight="400"
                              HorizontalScrollBarVisibility="Disabled"
                              Margin="0,0,0,0">
                    <StackPanel Width="400" Margin="0,0,0,10">
                        <TextBlock Text="👤 Фамилия" 
                                   Style="{DynamicResource FieldLabelStyle}"/>
                        <TextBox x:Name="RegSurnameTextBox" 
                                 Style="{DynamicResource BaseTextBoxStyle}"
                                 Margin="0,0,0,10"
                                 Width="380"/>

                        <TextBlock Text="👤 Имя" 
                                   Style="{DynamicResource FieldLabelStyle}"/>
                        <TextBox x:Name="RegNameTextBox" 
                                 Style="{DynamicResource BaseTextBoxStyle}"
                                 Margin="0,0,0,10"
                                 Width="380"/>

                        <TextBlock Text="👤 Отчество" 
                                   Style="{DynamicResource FieldLabelStyle}"/>
                        <TextBox x:Name="RegPatronymicTextBox" 
                                 Style="{DynamicResource BaseTextBoxStyle}"
                                 Margin="0,0,0,10"
                                 Width="380"/>

                        <TextBlock Text="📧 Электронная почта" 
                                   Style="{DynamicResource FieldLabelStyle}"/>
                        <TextBox x:Name="RegEmailTextBox" 
                                 Style="{DynamicResource BaseTextBoxStyle}"
                                 Margin="0,0,0,10"
                                 Width="380"/>

                        <TextBlock Text="📞 Телефон" 
                                   Style="{DynamicResource FieldLabelStyle}"/>
                        <TextBox x:Name="RegPhoneTextBox" 
                                 Style="{DynamicResource BaseTextBoxStyle}"
                                 Margin="0,0,0,10"
                                 Width="380"
                                 PreviewTextInput="RegPhoneTextBox_PreviewTextInput"
                                 MaxLength="15"/>

                        <TextBlock Text="👤 Логин" 
                                   Style="{DynamicResource FieldLabelStyle}"/>
                        <TextBox x:Name="RegLoginTextBox" 
                                 Style="{DynamicResource BaseTextBoxStyle}"
                                 Margin="0,0,0,10"
                                 Width="380"/>

                        <TextBlock Text="🔒 Пароль" 
                                   Style="{DynamicResource FieldLabelStyle}"
                                   Margin="0,0,0,5"/>

                        <!-- Поле пароля регистрации -->
                        <Grid Margin="0,0,0,10" Width="380">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <PasswordBox x:Name="RegPasswordBox" 
                                         Style="{DynamicResource BasePasswordBoxStyle}"
                                         Grid.Column="0"
                                         Width="335"/>

                            <TextBox x:Name="RegPasswordVisibleTextBox" 
                                     Style="{DynamicResource BaseTextBoxStyle}"
                                     Grid.Column="0"
                                     Width="335"
                                     Visibility="Collapsed"/>

                            <Button x:Name="ToggleRegPasswordButton" 
                                    Content="👁️" 
                                    Style="{DynamicResource TogglePasswordButtonStyle}"
                                    Grid.Column="1"
                                    Margin="8,0,0,0"
                                    Click="ToggleRegPasswordVisibility_Click"/>
                        </Grid>

                        <TextBlock Text="✓ Подтверждение пароля" 
                                   Style="{DynamicResource FieldLabelStyle}"
                                   Margin="0,0,0,5"/>

                        <!-- Поле подтверждения пароля -->
                        <Grid Margin="0,0,0,20" Width="380">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <PasswordBox x:Name="ConfirmPasswordBox" 
                                         Style="{DynamicResource BasePasswordBoxStyle}"
                                         Grid.Column="0"
                                         Width="335"/>

                            <TextBox x:Name="ConfirmPasswordVisibleTextBox" 
                                     Style="{DynamicResource BaseTextBoxStyle}"
                                     Grid.Column="0"
                                     Width="335"
                                     Visibility="Collapsed"/>

                            <Button x:Name="ToggleConfirmPasswordButton" 
                                    Content="👁️" 
                                    Style="{DynamicResource TogglePasswordButtonStyle}"
                                    Grid.Column="1"
                                    Margin="8,0,0,0"
                                    Click="ToggleConfirmPasswordVisibility_Click"/>
                        </Grid>

                        <Button x:Name="RegisterButton" 
                                Content="ЗАРЕГИСТРИРОВАТЬСЯ" 
                                Style="{DynamicResource RegisterButtonStyle}"
                                Margin="0,0,0,10"
                                Width="380"
                                Click="RegisterButton_Click"/>

                        <TextBlock Text="← Вернуться ко входу" 
                                   Style="{DynamicResource LinkTextStyle}"
                                   MouseLeftButtonDown="ShowLoginPanel"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </StackPanel>

        <!-- Сообщения об ошибках -->
        <TextBlock x:Name="MessageText" 
                   Grid.Row="3"
                   Text="" 
                   Style="{DynamicResource ErrorMessageStyle}"
                   Visibility="Collapsed"
                   Margin="0,10,0,0"/>
    </Grid>
</Window>